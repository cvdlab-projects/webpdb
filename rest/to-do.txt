Cambiamenti:

- istanza del db, ora viene caricato una volta sola e usata per le query.
- istanza del db viene caricata da file config.json esterno (contenente porta, host, ...).
- Implementata pagina admin: mancano i metodi propri per l'amministratore.
- Implementati filtri, in modo tale che solo gli utenti autenticati possono accedere a pagina admin.
- Aggiunto il controllo della sessione Web con Redis. (gestione cookie per l'autenticazione).
- Aggiunti nuovi routing (es /login, /logout, ecc ecc).

- Modificata leggermente l'interfaccia per renderla più chiara.